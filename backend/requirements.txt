# ── Web framework ──────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0

# ── Database ───────────────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# ── Google / Gmail ─────────────────────────────────────────
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.126.0

# ── NLP — HuggingFace (Level 1 AI) ────────────────────────
transformers>=4.40.0
torch>=2.2.0
sentencepiece>=0.2.0        # XLM-RoBERTa tokenizer
protobuf>=4.25.0

# ── LLM — Ollama (local) + Azure GPT-4o (premium) ────────
openai>=1.30.0              # Azure GPT-4o + Ollama compatible client

# ── MCP — Model Context Protocol (vrai protocole officiel) ─
mcp>=1.0.0                  # SDK MCP officiel Anthropic

# ── Vector DB + RAG ────────────────────────────────────────
chromadb>=1.0.0  # 0.5.0 casse avec numpy>=2.0 (np.float_ supprimé) — voir incident 03/09/2026
sentence-transformers>=3.0.0

# ── MLOps — Auto-Retraining Scheduler ─────────────────────
apscheduler>=3.10.0

# ── Async Task Queue ───────────────────────────────────────
celery>=5.3.0
redis>=5.0.0

# ── Auth — JWT + Password Hashing ──────────────────────────
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
cryptography>=42.0.0   # Fernet — encrypts connected-app tokens at rest (core/crypto.py)

# ── Email verification — vérification SMTP de la boîte à l'inscription ────────
dnspython>=2.6.0

# ── Speech-to-text — messages vocaux Slack/Teams (local, pas de dépendance ffmpeg) ─
faster-whisper>=1.0.0
